interface InitialWord {
  value: string;
  viewValue: string;
}

interface FinalLetter {
  value: string;
  viewValue: string;
}

interface SentenceData{
  comma: boolean;
  finalLetter: string;
  initialWord: string;
  name: string;
}
